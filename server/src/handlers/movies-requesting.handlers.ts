import {CustomRequestHandler} from 'types/utils';

// export const searchMovie: CustomRequestHandler = (req, res) => {
//   const {l, s, c, p, ps} = req.params;
//   const {originalUrl} = req;

// };
